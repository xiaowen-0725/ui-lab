# Adoption report — benchmark candidate

## Provenance and authority

- Route: UI Lab `replace` for a new operational Vite application.
- Authority: fixture-only technical binding (`vite-app`, `graphite`, `agent-workbench`, Chinese, light/dark, wide/collapse/narrow). This is not user approval of a product direction or visual master.
- CLI: current repository source CLI; config and lock were generated by `init`, `compose`, and `lock`. Lock was not hand-edited.
- Catalog order: Graphite Design System → Recipe → five required registry Blocks/components → application composition. No lookalike replacement was used.

## Contract mapping

| Contract | Mapping | Result |
|---|---|---|
| Stack | React 19, Vite, TS strict, Tailwind 4 | implemented |
| Theme | generated Graphite light/dark CSS variables | implemented |
| Font | platform system sans/mono; no proprietary asset | implemented |
| Icon | Lucide-only, 16px/2px standard controls | implemented |
| navigation slot | vendored `ThreadList` family | implemented |
| thread slot | vendored `AgentThread` family | implemented |
| composer slot | vendored `AgentComposer` family | implemented |
| artifact slot | vendored `ArtifactPanel` family | implemented |
| Recipe states | empty, streaming, approval, error | URL-addressable |
| benchmark states | default, loading | URL-addressable extension |
| responsive | Workbench 1200/768 boundaries and overlay/single-surface behavior | implemented/tested |
| assets | deterministic parking fixture, system fonts, Lucide | implemented |
| required | complete shell, selected Theme Kit, active `--wb-*` | implemented |
| forbidden | no generic dashboard replacement, token collapse, decorative state hiding, mixed icon/font family | manually checked |

## Evidence boundary

Strict audit is structural evidence only. Screenshots in `visual/implementation` are candidate regression captures. No approved reference is copied into `reference/`; therefore pixel comparison and human approval remain unavailable, and no fidelity, approval, or release claim is made.
